# Mobile

Le mobile n'est pas implémenté dans ce starter pack.

Recommandation : commencer par stabiliser la logique géométrique et l'éditeur 2D sur le web,
puis partager le moteur de calcul via `shared/src/` avant d'attaquer React Native / Expo.

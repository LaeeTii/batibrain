// Compatibilité temporaire avec le prototype : les nouveaux accès aux données
// doivent importer le client depuis `data/supabase`.
export { getSupabaseClient, hasSupabaseConfig } from '../data/supabase/client';
